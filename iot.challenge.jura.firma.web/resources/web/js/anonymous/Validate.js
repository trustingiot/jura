define( ["react"], function( React ) {
	return class Validate extends React.Component {
		constructor( props ) {
			super( props );
		}

		render() {
			return React.createElement( 'div', { className: 'firma' }, 'TODO' );
		}
	};
} );